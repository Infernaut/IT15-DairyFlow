@{
    ViewData["Title"] = "Usage Analytics";
}

<div class="container-fluid p-4">
    <div class="mb-4">
        <h2 class="mb-1">Usage Analytics</h2>
        <p class="text-muted mb-0">Adoption, engagement, and feature usage trends.</p>
    </div>

    <div class="card shadow-sm">
        <div class="card-body text-muted">
            No analytics provider configured yet.
        </div>
    </div>
</div>
